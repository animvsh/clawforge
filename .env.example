# Supabase public client config.
# Do not commit real keys. Keep local values in .env and deployment values in provider secrets.
VITE_SUPABASE_URL=https://mfslvyqvkutazsimsrhu.supabase.co
VITE_SUPABASE_ANON_KEY=replace-with-supabase-anon-key

# Optional provider/runtime secrets. These should be set only in local/deployment secret stores.
NVIDIA_API_KEY=
NVIDIA_NEMOTRON_MODEL=
MINIMAX_API_KEY=
MINIMAX_PLAN_KEY=
MINIMAX_MODEL=
AGENT_PROVIDER=mock
